{% extends 'base.html.twig' %}
{% block body %}

<div class="container">
  <table class="table">
    <tbody>
  <!--- TABLA CON ARTÍCULOS --->
    <div class="row">

     {% for articulosPorAutor in articulosPorAutor %}               
            <!--- FORMATO DE CADA ARTÍCULO --->
            <div class="col-sm-6 col-md-4">
              <div class="thumbnail">
                <img src="{{articulosPorAutor.imagen}}" style="width:350px;height:200px;">
                <div class="caption">
                  <h4>{{articulosPorAutor.articulo}}</h4>
                  <p>{{articulosPorAutor.body(200)}}</p>
                  <p><a href="/es/articulos/{{articulosPorAutor.id}}" class="btn btn-primary" role="button">Seguir leyendo</a></p>
                  <button type="button" class="btn btn-lg btn-info">{{autor.nombre}}</button>
                </div>
               </div>
            </div>
            <!--- FIN DEL FORMATO DE CADA ARTÍCULO --->
         {% endfor %}

    </div>
  <!--- FIN DE LA TABLA CON ARTÍCULOS --->

    </tbody>
  </table>
</div>

{% endblock %}